# 全章节具体关卡蓝图（主线 + 挑战线）

> 状态说明：
> - 本文件保留“课程层总蓝图”角色。
> - 当前近期实装与玩家层结构，以 `feasibility_layering.md` 为唯一权威判断。
> - 若本文件与 `feasibility_layering.md` 冲突，以后者为准。

本文件是当前课程重构的总蓝图。

说明：
- `复用`：现有关卡可直接沿用
- `重写`：现有关卡可保留骨架，但需明显调整
- `占位`：当前没有现成关卡，只定义设计位
- `半建模`：介于 solve 与 build 之间，要求孩子先处理部分题干信息再动手

---

## 主线闯关

## A0 真正的等号初见
目标：
- 先理解“等号 = 两边一样多”
- 不提前引入移项套路

细化稿：
- 见 `a0_blueprint_draft.md`

| 关卡 | 状态 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|
| A0-1 一样多吗 | 已设计 | 观察关 | 判断能否用等号连接 | 看图比较 | 不出现 `△` |
| A0-2 哪边不一样 | 已设计 | 判断关 | 找出不平衡的一组 | 读“多/少”关系 | 不出现移项 |
| A0-3 两边一起变 | 已设计 | 轻操作关 | 同时加 / 同时减 | 先读“同时”提示 | 为后续主线铺路 |

## A1 认识未知量
目标：
- 知道 `△` 代表不知道的数
- 先看懂“未知量 + 总量”关系

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| A1-1 糖果在哪里 | 重写 | 1-1 | solve-lite | 识别 `△` 与总量 | 先点出“谁是不知道的” | 现版动作太快 |
| A1-2 铅笔盒 | 重写 | 1-2 | solve-lite | 识别未知量和已知总量 | 先选“△ 表示什么” | 不应一上来就狂移项 |

## A2 加减变化与常数请走
目标：
- 理解增加 / 减少
- 理解和 `△` 同侧的常数需要被处理

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| A2-1 书架上的书 | 复用 | 1-3 | solve | 借走表示减少 | 读“借走” | |
| A2-2 攒零花钱 | 复用 | 2-1 | solve | 两个增加量分别处理 | 先点出“周一/周二” | |
| A2-3 多余的积木 | 复用 | 2-2 | solve | 同侧数字自动合并 | 先看“右边一共多少” | |
| A2-4 看图书 | 复用 | 2-3 | solve | 增减混合变化 | 先标记“借出/买了” | |
| A2-5 买文具 | 复用 | 2-4 | solve | 增减混合变化 | 先标记“获得/送出” | |

## A-Bridge 列式初见
目标：
- 第一次让孩子自己把故事部件拖成等式

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| A-Bridge-1 弹珠列式 | 复用 | 1-4 | build | 把基础故事放到两边 | 找总量与变化量 | |
| A-Bridge-2 送花列式 | 复用 | 1-5 | build | 处理减少项 | 先选符号 | |
| A-Bridge-3 甜甜圈列式 | 复用 | 2-5 | build | 多个正项放同侧 | 先读时间顺序 | |
| A-Bridge-4 红包列式 | 复用 | 2-6 | build | 正负项混合列式 | 先找“收到/花掉” | |

---

## B1 未知数的倍数与平均分
目标：
- 理解 `2△`、`3△`
- 体验几个一样的未知量
- 强化"平均分"而不是死记除法

> **合并说明**：原 B1（几个一样的未知数）和 B2（平均分与乘除收尾）使用完全相同的 3-1、3-2、3-3 关卡，属于重复计数。现合并为一个单元，3 关不变。

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| B1-1 同价铅笔 | 复用 | 3-1 | solve | `2△ = 6` 类结构，÷2 平均分 | 读"每支同价" | |
| B1-2 铅笔和橡皮 | 复用 | 3-2 | solve | 先移常数再平均分 | 读"另加一个固定量" | |
| B1-3 团购 T 恤 | 复用 | 3-3 | solve | 系数更大，÷3 | 读"3 件同价" | |

## B3 半份与分数系数
目标：
- 从整份走向半份、三分之一、几分之几

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| B3-1 分蛋糕 | 复用 | 4-1 | solve | 半块变整块 | 读“半块” | |
| B3-2 半价加包装 | 复用 | 4-2 | solve | 分数系数 + 常数 | 读“半价 + 包装” | |
| B3-3 三分之一盒巧克力 | 复用 | 4-3 | solve | `1/3△ + 1` | 读“只是一部分” | |
| B3-4 一块半蛋糕 | 复用 | 4-4 | solve | `3/2△` | 读“一个半” | 稍拔高 |

## B-Bridge 乘法 / 分数列式
目标：
- 把乘法、分数型故事也能写成等式

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| B-Bridge-1 买苹果列式 | 复用 | 3-4 | build | `2△` 列式 | 先找“每袋一样多” | |
| B-Bridge-2 运动会列式 | 复用 | 3-5 | build | 系数与常数分离 | 先找“每圈 + 奖励” | |
| B-Bridge-3 半价优惠列式 | 复用 | 4-5 | build | 分数型列式 | 先找“半价” | |

---

## C1 故事到等式
目标：
- 把故事稳定翻译成等式

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| C1-1 数糖果 | 复用 | 5-1 | build | 最基础故事列式 | 先找“原来/一共” | |
| C1-2 借书 | 复用 | 5-2 | build | 减法故事列式 | 先找“借走/剩下” | |
| C1-3 攒零花钱 | 复用 | 5-3 | build | 多项故事列式 | 先找时间顺序 | |
| C1-4 买铅笔 | 复用 | 5-4 | build | 系数故事列式 | 先找“每支一样” | |
| C1-5 运动会得分 | 复用 | 5-5 | build | 系数 + 常数列式 | 先找“每次 + 额外” | |
| C1-6 三瓶饮料 | 复用 | 5-6 | build | 完整建模链路 | 先找“总量关系” | 作为单元收束 |

## C2 选符号与干扰条件
目标：
- 不被多余卡片骗
- 能根据题意确定正负

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| C2-1 谁的弹珠多 | 复用 | 5-7 | build | 去掉无用卡 | 读“多多少” | |
| C2-2 课外班 | 复用 | 5-8 | build | 识别干扰条件 | 读“哪些条件真的有关” | |
| C2-3 买铅笔（选符号） | 复用 | 5-9 | build | 选正负号 | 读“买/花掉” | |
| C2-4 赚了又花了（选符号） | 复用 | 5-10 | build | 选正负号 | 读“赚/花” | |

## C3 选择未知量
目标：
- 让孩子开始学会“设什么最合适”

细化稿：
- 见 `c3_blueprint_draft.md`

| 关卡 | 状态 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|
| C3-1 谁是未知量 | 已设计 | 半建模 | 先从题干中圈出真正不知道的量 | 选未知量 | 课程层保留；玩家层第一版已用 build 变体近似实装 |
| C3-2 设小的还是设大的 | 已设计 | 半建模 | 比较两种设法 | 读“谁更顺手” | 课程层保留；玩家层第一版已用 build 变体近似实装 |
| C3-3 为什么设一个就够 | 已设计 | 半建模 | 用和差倍轻量例子说明“一设一带” | 读比较关系 | 课程层保留；玩家层第一版已用 build 变体近似实装 |

## C4 方程工坊入门
目标：
- 从给定故事一步步进入现有等式引擎

> 当前状态：
> - 课程层保留。
> - 玩家层与实现层已冻结，不进入近期范围。

细化稿：
- 见 `c4_blueprint_draft.md`

| 关卡 | 状态 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|
| C4-1 工坊：选未知量 | 已设计 | guided-workshop | 选 `△` 代表谁 | 必须先读故事 | 远期冻结 |
| C4-2 工坊：选关系卡 | 已设计 | guided-workshop | 选择“总量 / 剩下 / 比……多 / 每份一样” | 读关系词 | 远期冻结 |
| C4-3 工坊：生成并求解 | 已设计 | guided-workshop | 把选项生成等式再求解 | 读回故事并验证 | 第一版工坊收束，远期冻结 |

---

## D1 括号是一整包

> **当前结论**：D1 已正式落位于“第三程与第四程之间的独立过渡段”。本文件保留详细关卡，不再把它视为待定项。见 `feasibility_layering.md §3.2`。

目标：
- 理解一整包、展开、再整理

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| D1-1 两袋礼包 | 复用 | 6-1 | solve | 括号初见 | 读“每袋都有” | |
| D1-2 零食大礼包 | 复用 | 6-2 | solve | 展开后移项 | 读“礼包 + 单独项” | |
| D1-3 三盒彩笔 | 复用 | 6-3 | solve | 更多倍数的括号 | 读“每盒一样” | |
| D1-4 文具盒 | 复用 | 6-4 | solve | 括号综合整理 | 读“先看整包再看单独项” | |

## D2 两边都有未知数
目标：
- 把未知量集中到同一边

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| D2-1 零花钱比较 | 复用 | 7-1 | solve | 双边未知数初见 | 读“谁比谁多” | |
| D2-2 苹果一样多 | 复用 | 7-2 | solve | 右边系数更大 | 读“相同数量” | |
| D2-3 文具店 | 复用 | 7-3 | solve | 双边未知数 + 常数 | 读“价格比较” | |
| D2-4 手工花 | 复用 | 7-4 | solve | 括号 + 双边未知数 | 读“每份关系” | |
| D2-5 教室人数 | 复用 | 7-5 | solve | 主线高峰 | 读“多人关系” | 主线压轴 |

## D3 主线方法地图复习
目标：
- 回顾前面已经学过的方法与入口

> 当前状态：
> - 课程层保留“方法地图复习”这一教学职能。
> - 玩家层第一版不再单独成章，而是并入 `D4` 尾声或阶段结算节点。
> - 实现层近期不单独做 `mixed` 新类型。

细化稿：
- 见 `d3_blueprint_draft.md`

| 关卡 | 状态 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|
| D3-1 关系复习 | 已设计 | mixed | 从等号、未知量到关系词回顾 | 轻读题动作 | 课程层保留，玩家层第一版并入 D4 尾声 |
| D3-2 路线选择复习 | 已设计 | mixed | 判断这题先直接变形、先选未知量还是先看线段图 | 方法选择动作 | 课程层保留，玩家层第一版并入 D4 尾声 |

## D4 线段图桥接
目标：
- 把线段图作为从故事关系到方程的辅助视图

> 当前状态：
> - 课程层保留。
> - 玩家层与实现层第一版冻结，不进入近期范围。

细化稿：
- 见 `d4_blueprint_draft.md`

| 关卡 | 状态 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|
| D4-1 比较关系先画线段 | 已设计 | assisted-build | 先看比较图，再写关系 | 读“比……多 / 少” | 第一批只做比较关系，远期冻结 |
| D4-2 总量 + 差量的线段图 | 已设计 | assisted-build | 先看总量与差量图，再写式子 | 读“和 / 差” | 远期冻结 |
| D4-3 从线段图到等式 | 已设计 | assisted-build | 从图跳到等式引擎 | 读图回到故事 | 作为桥接收束，远期冻结 |

---

## 挑战线

## T1 逆向思考
目标：
- 通过神秘机器和倒推，延展等式思维

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| T1-1 神秘机器一号 | 复用 | 8-1 | solve | 逆向思考初见 | 读“先……后……” | |
| T1-2 神秘机器二号 | 复用 | 8-2 | solve | 分数系数逆向 | 读运算链条 | |
| T1-3 神秘机器三号 | 复用 | 8-3 | solve | 先减后乘 | 读运算顺序 | |
| T1-Bridge-1 逆向列式 | 复用 | 8-4 | build | 从过程列式 | 读机器步骤 | |
| T1-Bridge-2 迷惑数字 | 复用 | 8-5 | build | 去掉无关信息 | 读“哪些步骤有用” | |
| T1-Bridge-3 先减后加（选符号） | 复用 | 8-6 | build | 逆向 + 选符号 | 读“减”和“加” | |

## T2 和差倍问题
目标：
- 用一元思路处理和差倍关系

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| T2-1 和是20差是4 | 复用 | 9-1 | build | 设小的为 `△` | 读“和 / 差” | |
| T2-2 哥哥的糖 | 复用 | 9-2 | build | 倍数关系 | 读“是……的几倍” | |
| T2-3 大数和小数 | 复用 | 9-3 | build | 差量关系 | 读“大 / 小 / 多多少” | |
| T2-4 姐弟零花钱 | 复用 | 9-4 | build | 倍数 + 差量 | 读关系链 | |
| T2-5 多余条件 | 复用 | 9-5 | build | 排除干扰条件 | 读“哪些条件无用” | |
| T2-6 姐弟攒钱（选符号） | 复用 | 9-6 | build | 比较关系与符号 | 读“多 / 少 / 翻倍” | |

## T3 年龄问题
目标：
- 体验时间在变、关系不变

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| T3-1 今年和5年后 | 复用 | 10-1 | build | 时间平移 | 读“今年 / 5 年后” | |
| T3-2 爸爸比我大 | 复用 | 10-2 | build | 差量不变 | 读“比……大” | |
| T3-3 哥弟年龄和 | 复用 | 10-3 | build | 倍数年龄 | 读“哥哥是弟弟的几倍” | |

## T4 盈亏与平均数
目标：
- 把分配、盈亏、平均关系转成方程

| 关卡 | 状态 | 当前来源 | 类型 | 核心动作 | 读题动作 | 备注 |
|---|---|---|---|---|---|---|
| T4-1 分糖果 | 复用 | 11-1 | build | 盈亏初见 | 读“两种分法” | |
| T4-2 分组坐 | 复用 | 11-2 | build | 分组问题 | 读“每组多少 / 还剩或还缺” | |
| T4-3 装球 | 复用 | 11-3 | build | 总量关系 | 读“装了多少 / 剩多少” | |
| T4-4 平均每盒 | 复用 | 11-4 | build | 平均数结构 | 读“每盒平均” | |
| T4-5 多余数据 | 复用 | 11-5 | build | 干扰数据 | 读“哪些信息没用” | |

---

### 总体校验标准

- 每个单元至少有一个清晰目标
- 每个关卡都应有“读题动作”说明
- build 关不再是第一次真正要求读题的地方
- 占位符已显式标注，不假装现有内容足够

---

### 当前不做的事

- 不在这一步修改 `levels.js`
- 不在这一步实装新导航
- 不在这一步决定线段图具体交互
- 不在这一步补完所有占位关题面
